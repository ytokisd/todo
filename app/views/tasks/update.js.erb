$(' <%= "#update_task_#{@task.id}" %>').remove();
$(' <%= "#task_view_#{@project.id}" %>').hide();
$(' <%= "#create_task_#{@project.id}" %>').remove();
$(' <%= "#task_form_#{@project.id}" %>').remove();
$('.js-project-<%= @project.id %>-tasks').html('<%= j render("tasks/task", project: @project) %>');